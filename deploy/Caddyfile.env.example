# Copy beside the Caddyfile or export before `caddy run`.
# No secrets. Hostname and Cloud Auth host are supplied at provision.

MEDVERSE_ORIGIN=lms.example.com
SUPABASE_AUTH_HOST=REPLACE_PROJECT_REF.supabase.co
NEXT_UPSTREAM=127.0.0.1:3000
POSTGREST_UPSTREAM=127.0.0.1:3001
